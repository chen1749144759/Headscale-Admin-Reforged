# ScaleForge 文档

这份历史长文档已经停止维护。旧注册流程和旧客户端命令不再符合当前账户密码架构，继续照旧文档部署会导致客户端无法登录。

请统一阅读[项目根 README](../README.md)，其中包含当前架构、账户登录、私有 UDS、验证码、DNS、签名 OTA、数据库迁移、首次部署和保留数据升级步骤。

新客户端使用 `scaletail login --username ...`；自动化场景只允许通过权限为 `0600` 的文件配合 `--password-file` 登录。
